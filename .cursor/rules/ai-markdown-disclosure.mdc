---
description: Enforce AI-generated marker at top of markdown files
alwaysApply: true
---

# AI 生成 Markdown 标识

对所有 AI 生成或修改的 Markdown 文件（`*.md`、`*.mdx`、`*.mdc`）强制执行以下规则：

1. 文档第一行必须为：`> AI GENERATED: 该文档由 AI 生成或修改。`
2. 标识必须位于最开头，不能放在标题之后。
3. 仅当用户明确要求不添加标识时，才允许例外。

示例：

```markdown
> AI GENERATED: 该文档由 AI 生成或修改。

# 标题
```
