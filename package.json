{
  "name": "zhuanlema-cloudfunctions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud functions test runner for Zhuanlema",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
